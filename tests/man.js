/**
 *
 *
 *
 * This is the primary header description
 * @version 0.0.1
 * @author Nicholas Riley
 *  -email: nick@sparkida.com
 * @global
 * @namespace
 * @name Spk
 */
(function (w) {

    /**
     * 
     * @name a
     */
    var a = function(){
    };

    /**
     * This is the first test method description
     * @instance Spk
     * @name test
     * @param {string='foo'|bool} testVar1 - The first test variable.
     * @param {object|array} testVar2 - An object, or an array.
     * @usage Add new script to head element: Spk.test( testVar1 )
     * @rel Spk.node, Spk.load
     * @example Create new script object and add to page.
     * //create new script element
     * var elem = Spk.node('script');
     * //set source
     * elem.src = "http://mysource.com/js/spk/test.js";
     * //add script to page
     * tpk.addScript(elem);
     * @example create nothing
     * var n = null
     */
    a.test = function(testVar1, testVar2){
    };
    /**
     * This is the first test method description
     * @instance Spk
     * @name test
     * @param {string|bool} testVar1 - The first test variable.
     * @param {object|array} testVar2 - An object, or an array.
     * @usage Add new script to head element: Spk.test( testVar1 )
     * @usage Add new script to head element: Spk.test( testVar1, testVar2 )
     * @rel Spk.node
     * @rel Spk.load
     * @example Create new script object and add to page.
     * /-create new script element
     * this is a document comment
     * it should automatically be edited
     * for us-/
     * var elem = Spk.node('script');
     *     script = function(){
     *         /-this is a comment inside another comment
     *         test
     *         -/
     *         return 'asn';
     *     };
     * //set source
     * elem.src = "http://mysource.com/js/spk/test.js";
     * //add script to page
     * Spk.addScript(elem);
     * @info some info about this batch
     * and continued info
     * @info some info about this batch
     * and continued info
     */
    a.test = function(testVar1, testVar2){
        var asf = "/- qasfq fqwf -/"
    };
}(window))

